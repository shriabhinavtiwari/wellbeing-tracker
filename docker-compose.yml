
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=mongodb+srv://donotgetinmyway:eQMqmVWA6odw2amk@cluster0.szfxjhj.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - SECRET_KEY=your-secret-key-change-this-in-production
    volumes:
      - ./backend:/app
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:8000
    restart: unless-stopped
